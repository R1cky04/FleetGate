{
  "name": "fleetgate-desktop",
  "version": "1.0.0",
  "description": "FleetGate - Vehicle Rental Management System (Desktop)",
  "main": "dist-electron/main.js",
  "scripts": {
    "dev": "vite",
    "build": "node build-electron.js && vite build",
    "preview": "vite preview",
    "electron": "electron .",
    "electron-dev": "node build-electron.js && vite & electron .",
    "electron-build": "node build-electron.js && vite build && electron-builder",
    "build:electron": "electron-builder --win --publish never"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "electron": "^27.0.0",
    "pinia": "^2.1.6",
    "vue": "^3.3.4",
    "vue-router": "^4.2.5"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@vitejs/plugin-vue": "^4.5.0",
    "electron-builder": "^24.6.4",
    "esbuild": "^0.21.5",
    "typescript": "^5.3.0",
    "vite": "^5.0.0"
  }
}
